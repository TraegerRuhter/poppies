# poppies

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues on `TraegerRuhter/poppies`, driven via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — one `CONTEXT.md` and `docs/adr/` at the repo root, both created lazily by `/domain-modeling` rather than upfront. See `docs/agents/domain.md`.
